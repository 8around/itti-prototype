# 202608 — 목업 정렬 리핏: 목업 앱 UI + 실제 DART 데이터

**상태** 계획 확정 · 구현 미착수
**작성일** 2026-08-12
**선행 계획** [`prototype-plan.md`](./prototype-plan.md) (T0~T7+T10, 구현 완료 — main `895e0e6`)
**명명 규칙** 이 문서부터 계획 문서는 `YYYYMM-` 숫자 prefix를 붙인다

---

## Context

**무엇이 어긋났나.** 완료된 프로토타입(T0~T10)은 데이터 파이프라인과 검증 화면(유니버스 표·삼성vsKB 비교·킷친싱크) 중심이었다. 그러나 실제로 원했던 것은 **클라이언트가 제공한 목업(`이띠_R1_배포앱_목업_6.html`, 투자자 앱 화면)과 유사한 웹**에 실제 DART 데이터를 채워 보여주는 것이었다.

**무엇을 살리나.** 데이터 레이어는 전부 재사용한다 — 스냅샷 체계(467건+확장), 정규화 엔진(Resolution·displayState 6종), 프로필 카탈로그, SourcePanel(collapse), 차트 프리미티브 8종(목업 CSS에서 이식했으므로 그대로 맞음), 토큰(globals.css). **프레젠테이션 계층만 목업 기준으로 재구성**한다.

## 확정 사항 (사용자 결정)

| 항목 | 결정 |
|---|---|
| 화면 | **재무 코어 10화면** (목업 01~10). 공시추적·IR조르기 등 백엔드 없는 7화면 제외 |
| 분기 데이터 | **추가 수집** — ⚠️ **현재 2026년 8월**: "최근 5분기" = 2025Q1~2026Q1 (+2026 반기는 공시돼 있으면 포함) |
| 기존 개발자 화면 | **`/dev/*`로 이사** |
| 폼팩터 | **반응형 모바일 웹** (max-width ~430px 중앙 칼럼 + 하단 GNB). PWA 언급은 무시 |
| 실행 방식 | subagent-driven development (구현 승인 후) |

## 새 입력 — `금융업 발라내기.xlsx`

`ete-django/docs/goals-in-2months/` 소재. 클라이언트 수작업 매핑(한경에이셀 양식 기반).

- 금융사 손익을 **총액(gross) 구성**으로 본다: 영업수익(이자수익+보험수익+수수료수익+대손충당금환입) − 영업비용(이자비용+보험비용+지급수수료+대손상각비+판관비) → 영업이익 → **지배주주 당기순이익**
- 현재 구현(순액 3개 스택)과 다름 → **금융 프로필 카탈로그를 이 구조로 재정렬** (N1)
- BS 세부(현금및예치금·유가증권·대출채권·예수부채·책임준비금 등)와 PER/PBR/ROE/DPS도 포함 — 시세 계열은 여전히 원천 미확보로 정직 표기
- 시트: Cover / **FS-Q(분기)** / 금융지주사_업무Flow / 증권사_업무Flow / FS-A(연간)

## 슬랙 스레드 반영사항 (p1786297665534189 — 2026-08-12 정오까지 전문 확인, 신규 7건 포함)

① **차트 형식 클라이언트 합의 확정** (이주호 제안 → 김예지 *"차트 표시부분은 이견없습니다. 진행 부탁드립니다"*). **목업과 다른 지점은 이 합의가 우선한다:**

| 데이터 | 합의된 차트 | 우리 프리미티브 |
|---|---|---|
| 금액 추이 (매출·영업이익·순이익·EPS) | **분기별 막대** | QuarterBars ✓ |
| **성장률·비율 추이 (QoQ·YoY·ROE·부채비율)** | **꺾은선** ← 목업 05는 발산막대였음 | LineChart (color prop 확장) |
| 재무상태 (자산=부채+자본) | 누적 막대 | StackedBar100 ✓ |
| 현금흐름 (영업/투자/재무) | **상하(±) 막대** ← 목업 06은 가로 발산 | ZeroAxisBars 변형(세로 ±) |
| 공통 규칙 | 손실 분기 기준선 아래 · 잠정치 점선(확정 시 갱신) · 적자 PER N/A · **배당 0원 vs 데이터 없음 구분** | 전부 기존 displayState 체계와 일치 ✓ |

② **금융/비금융 분리 작업 + 금융이 작업 순서 마지막** (김예지 8/12 08:38 → 12:00 정정 문답): *"전종목을 모두 8주 안에 구현 후 배포인데, 금융이 작업 순서가 가장 마지막"*. → 화면 태스크 내부 순서도 **비금융 경로 먼저 완성 → 금융 분기 마지막**으로 배치

③ 비금융은 손익구조 동일, **업종별 참고 지표만 다름** — 오버레이·부문별 지표의 표시 형태 리스트는 클라이언트가 **내부 논의 후 추후 송부** 예정. BIO 오버레이(화면 10)는 목업 기준으로 만들되 "클라이언트 확정 리스트 대기" 각주

④ 양준호: 금융사 간 표기 차이 → 발라내기 xlsx가 정답지 / 김예지: 사업부문 매출은 DART API 미제공 → 화면 03 파이는 `원천 미확보 [사업보고서 원문 필요]` 정직 표기

---

## 목업 10화면 × 데이터 매핑

✅=실데이터 있음 ◑=추가 수집·계정 후보 필요 ✖=원천 미확보(정직 상태 표기)

| # | 화면 | 구성 요소 | 데이터 |
|---|---|---|---|
| 01 | 홈 | 관심종목 카드(유니버스 20종목), 히어로 | ✅ 재무 대표지표 / ✖ 종가·등락%(시세 미연동 소형 표기) / 공시추적 미니·D-day는 범위 밖 → 카드 생략 |
| 02 | 종목 상세 | `.val` 3분할 스트립 + 개황 KV | ✅ 개황(company) / ✖ 종가·시총·PER·PBR |
| 03 | 재무 메인보드① | 사업부문 파이 · 매출 5분기 막대 · 비교행(전년동기/직전분기) | ✖ 사업부문 파이 / ◑ 5분기(신규 수집) / ✅ 비교행(YoY·QoQ 계산) / ✖ 증권사추정 |
| 04 | 재무 메인보드② | 영업이익 5Q · 순이익 5Q(0축) · EPS 꺾은선 | ◑ 전부 분기 수집 후. 순이익은 **지배주주 기준** |
| 05 | 재무 메인보드③ | 매출 QoQ 추이 · 요약타일4 · 최대주주 지분 스택 · 수주잔고 | ◑ QoQ(꺾은선)·타일 / ◑ 최대주주(hyslrSttus 신규 수집) / ✖ 수주잔고 |
| 06 | IR VIS 손익·현금흐름 | 워터폴 · 2계열 그룹막대 · CF ± 막대 · 기간비교(그룹막대+이익률 꺾은선+FCF) | ✅ 전부 (LineChart 색 prop 확장 필요) |
| 07 | 재무상태 | 자산=부채+자본 스택 · 자산 구성 5분할 · 차입금 만기 2분할 | ✅ 총계 / ◑ 구성 5분할·차입금(AcntAll 계정 후보 추가) |
| 08 | 안정성 | KPI4 · 현금vs차입 대비막대 · 이자보상배율 게이지(1배 임계) · peer 비교 · 5기간 표+스파크라인 | ◑ 이자보상배율(이자비용 후보 추가, 자체 계산) / peer 중앙값은 20종목 내 동일 프로필로 대체 표기 / ✅ 표 |
| 09 | 수익성·성장성·주주환원 | KPI 12 · 매출 YoY · 밸류에이션 · 6행 표 | ✅ 대부분 확보 / ✖ 밸류에이션 4종 |
| 10 | 바이오 오버레이 | 업종 콜아웃 · R&D/매출 · 런웨이 · 분기 현금 순유출 막대 · CB | ◑ R&D비·현금성자산 후보 추가, 런웨이=현금성÷분기순유출 계산 / 대상: 헬릭스미스·신라젠 (오버레이 축: BIO) |

**금융 종목(7종)의 03~06 대체**: 발라내기 구조 — 영업수익 구성(이자·보험·수수료수익 gross 스택) → 영업비용 → 영업이익 → 지배주주 순이익. 화면 07 BS 세부도 금융 전용 후보(대출채권·예수부채 등) 시도, 없으면 MISSING.

---

## 태스크 (SDD 실행 단위)

### N0 — 스냅샷 확장 수집
- `fnlttSinglAcntAll` 추가 매트릭스: 20종목 × {11013, 11012} × {2023,2024,2025} + **2026 × {11013, 11012}** × CFS→OFS 폴백. 2026 11012는 다수 `013` 예상(8/14 마감 전) — 정상 기록
- `hyslrSttus`(최대주주) 20종목 × 2025 × 11011 신규
- 예상 호출 ~250회 (일일 한도 1.3%). 기존 fetch 스크립트는 데이터 주도라 배열 확장만
- 완료: manifest 갱신 리포트, 2026 데이터 실재 종목 수 보고

### N1 — 정규화·카탈로그 확장 (발라내기 정렬)
- **분기 시계열 빌더**: reprt_code별 단일분기 값(`thstrm_amount`) + Q4 역산 → "최근 N분기(기본 5)" 선택 로직. 최신 분기 동적 판정(2026Q2 있으면 포함). x축 라벨 `25.1Q…26.1Q`, 역산 분기는 provisional
- **신규 AcntAll 후보** (전부 실측 스팟 확인 후 확정): 자산구성 5분할(현금성·매출채권·재고·유형·기타), 단기/장기차입금, 이자비용(이자보상배율용), 경상연구개발비(R&D), 현금및현금성자산 / 금융 gross: 이자수익(기존)·보험수익(기존)·수수료수익·영업비용 계열·금융BS(대출채권·예수부채·책임준비금)
- **발라내기 xlsx 대조표**: 시트 FS-Q/FS-A 항목 ↔ DART account_id 매핑 표를 브리프에 제공(컨트롤러가 xlsx 파싱), 구현자는 실측 HIT율 보고. 금융 프로필 pnl을 gross 구조로 재편(`chart:"stacked"`=수익 구성, 비용은 참고, headline=지배주주)
- **BIO 오버레이 프로필**: STANDARD 위에 업종 오버레이 개념 첫 구현(profile과 별개 축 — `overlay:"BIO"` 유니버스 플래그)
- `pnpm snapshot:build` + vitest 확장 (5분기 시계열·런웨이 검산 케이스)

### N2 — 앱 셸 + /dev 이사
- 모바일 중앙 칼럼(max-width 430px) 레이아웃 + 하단 GNB(목업 5탭 — 홈·종목 활성, 나머지 3탭 자리만+`범위 외` 툴팁) + 상단 앱바
- 기존 화면 이동: `/universe`·`/compare/pnl`·`/kitchen-sink`·`/debug*` → `/dev/*` (리다이렉트 불필요, 로컬 전용)
- 목업 폰트·토큰 재확인 (이미 이식됨), `.asof`·`.srcfoot` 등 공통 조각 컴포넌트화

### N3 — 화면 01·02 (홈 + 종목 상세)
- 홈: 관심종목 카드(20종목 전체를 "관심종목"으로 간주), 시세 필드는 `시세 미연동` 뱃지
- 종목 상세: 목업 02의 레이아웃 + 03~10 섹션으로 이어지는 세로 스크롤 구조(목업은 화면 분할이지만 웹은 단일 상세 페이지 내 섹션 + 섹션 인덱스 칩 `.vindex` 재현)
- 라우팅: `/stock/[code]`를 목업 스타일로 전면 재작성 (기존 7섹션 페이지 대체)

### N4 — 화면 03·04·05 (재무 메인보드①②③) — **비금융 경로 먼저**
5분기 막대(비교행 포함)·0축 순이익(지배)·EPS 꺾은선·**QoQ 성장률은 꺾은선**(스레드 합의 — 목업의 발산막대 대체)·요약타일·최대주주 지분·`원천 미확보` 카드(사업부문·수주잔고). **금융 종목 분기(발라내기 gross 구성)는 이 태스크의 마지막 단계** (스레드 합의 ②)

### N5 — 화면 06 (손익·현금흐름 IR VIS)
워터폴(재사용)+그룹막대+**현금흐름 상하(±) 세로 막대**(스레드 합의 — 목업의 가로 발산 대체, ZeroAxisBars 변형)+기간비교. LineChart에 color prop 추가(이익률=주황)

### N6 — 화면 07·08 (재무상태·안정성)
자산 구성 5분할 스택·차입금 만기·이자보상배율 게이지(1배 적색 임계선 — 신규 프리미티브 `<Gauge>`)·**비율 추이(부채비율 등)는 꺾은선**(스레드 합의)·peer 비교(동일 프로필 중앙값, 표본 수 명시)·5기간 비교표+스파크라인(`<Sparkline>` 신규 또는 LineChart 소형화). 금융 분기 마지막

### N7 — 화면 09·10 (수익성·주주환원 + 바이오 오버레이)
KPI 그리드·**ROE 등 비율 추이 꺾은선**·6행 표. BIO 오버레이: R&D/매출·런웨이·분기 현금 순유출·PER "해당없음(영업손실)" 콜아웃 — 헬릭스미스·신라젠에서 자동 발동. 오버레이 표시 형태는 "클라이언트 확정 리스트 대기" 각주 부착 (스레드 합의 ③)

### N8 — 최종 전체 리뷰 (opus) + 픽스 웨이브 1회
목업 육안 대조(스크린샷) 포함. 브라우저 콘솔 확인은 컨트롤러가 Chrome으로 직접

**의존**: N0→N1→(N2 병렬 가능)→N3→N4→N5→N6→N7→N8. 모든 수치는 derived.json 유래(하드코딩 0), 모든 카드에 SourcePanel, 결측=데이터 없음 원칙 유지.

---

## 검증

1. `pnpm snapshot:fetch --dry-run` → 확장 매트릭스 확인 후 실수집, 2026 데이터 보고
2. `pnpm vitest run` — 기존 81 + 신규(5분기 선택·런웨이·gross 스택) 전부 통과
3. `pnpm dev` 브라우저(Chrome, 모바일 뷰포트 390px):
   - `/` 홈 카드 → `/stock/005930` 목업 02~09 섹션 스크롤, 5분기 막대의 마지막 막대가 26.1Q(또는 26.2Q)
   - `/stock/105560`(KB) — 발라내기 gross 구성(이자수익 30.49조 등) 스택
   - `/stock/084990`(헬릭스미스) — BIO 오버레이 콜아웃 + 런웨이
   - `/dev/universe` 등 기존 화면 정상
   - 목업 HTML을 옆 탭에 띄워 화면별 육안 대조
   - 콘솔 에러 0
4. `원천 미확보` 카드가 사업부문·수주잔고·시세 4종에 정확히 표기 (근거 없는 수치 0)
5. **스레드 합의 차트 형식 준수**: 금액=분기 막대 · 성장률/비율=꺾은선 · 재무상태=누적 막대 · 현금흐름=상하(±) 막대 · 손실 분기 기준선 아래 — 화면별 대조

## 참조

- 목업: [`이띠_R1_배포앱_목업_6.html`](../../../ete-django/docs/goals-in-2months/이띠_R1_배포앱_목업_6.html) (읽기 전용)
- 발라내기: `../../../ete-django/docs/goals-in-2months/금융업 발라내기.xlsx`
- DART 명세: [`dart-api/`](../../../ete-django/docs/dart-api/) 9종
- 선행 계획·완료 기록: [`prototype-plan.md`](./prototype-plan.md) (main `895e0e6`, 리뷰 클린)
